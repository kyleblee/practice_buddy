<h2>My Backing Tracks</h2>

<% @backing_tracks.each do |bt| %>
  

<% end %>

<%= button_to "Add a new backing track", new_user_backing_track_path(@user), method: 'get' %><br>

<%= button_to "My Backing Tracks", user_backing_tracks_path(@user), method: 'get' %>
